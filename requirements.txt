# ---- Core numerics ---------------------------------------------------------
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ---- NLP / text ------------------------------------------------------------
nltk>=3.8
rapidfuzz>=3.0.0
# Qwen2.5's tokenizer falls back to a slow path that needs protobuf +
# sentencepiece. Without them AutoTokenizer.from_pretrained raises
# ImportError which the caller mis-reports as "Cache miss".
protobuf>=3.20
sentencepiece>=0.1.99

# ---- Hugging Face stack ----------------------------------------------------
# torch MUST be >= 2.4 for current transformers (`set_module_tensor_to_device`
# has `non_blocking=` kwarg only there). On Windows, install the CPU-only
# wheel: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4
accelerate>=0.33.0
transformers>=4.46.0
sentence-transformers>=3.0.0
huggingface_hub>=0.24.0
datasets>=2.14.0

# ---- Retrieval UI ----------------------------------------------------------
# Gradio 6.0 removed gr.Plot.select() which the cluster scatter click-bridge
# expects. Stay on 5.x.
gradio>=5.0,<6.0
plotly>=5.15.0

# ---- Creative generators ---------------------------------------------------
diffusers>=0.32.0
Pillow>=10.0.0

# ---- Misc ------------------------------------------------------------------
requests>=2.31.0
charset_normalizer>=3.0.0
