__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/
data/
cache/
.ipynb_checkpoints/
.DS_Store

# Ignore HuggingFace caches - the AI weights are too large for GitHub (multi-gigabytes)
# and their aggressive internal symlink structure crashes Git on macOS/Windows.
# Your professor will just run the bundle script or let the models download on first boot.
models/hub/
